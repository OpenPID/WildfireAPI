{
  "name": "wildfire_api",
  "version": "1.0.0",
  "description": "API for registering users for wildfire notifications",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "OpenPID",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^4.11.12",
    "restify": "^6.0.1"
  }
}
